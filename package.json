{
  "name": "aiman-b2c-assets",
  "version": "0.0.0",
  "private": true,
  "description": "Offline regression tests for immutable AIMAN B2C template releases",
  "scripts": {
    "test:unit": "node --test tests/theme-script.test.cjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.58.1"
  }
}
